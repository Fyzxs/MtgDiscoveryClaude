---
paths:
  - "csharp/src/Lib.Adapters/**/Queries/Entities/*"
  - "csharp/src/Lib.Adapters/**/Commands/Entities/*"
---

The internal implementation of an `XferEntity` to transfer the information from the `ItrEntity` to the Adapter layer.
Each of these will have an associated Mapper to populate the `XferEntity` from the `ItrEntity`.
